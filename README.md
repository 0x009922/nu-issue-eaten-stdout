# nu-issue-eaten-stdout

Run:

```bash
nu ./start.nu
```

**Expected**: you can see output of internally started `looper.nu` script.

**Actual**: you don't see it until you halt the script with <kbd>Ctrl+C</kbd>.
